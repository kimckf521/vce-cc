# dependencies
node_modules/

# next.js
.next/
out/

# env files (never commit secrets)
.env
.env.local
.env.*.local

# debug
npm-debug.log*

# misc
.DS_Store
*.pem
.figure-artifacts/

# exams PDFs are large — store in Supabase Storage. Root-anchored (leading
# slash) so this only ignores the top-level /exams PDF folder and NOT app
# route directories named "exams" (e.g. app/(content)/.../exams/).
/exams/

# prisma generated
prisma/generated/

# extracted question JSON & python cache
scripts/output/
scripts/__pycache__/

# textbook PDFs (too large for git) — root-anchored, same reasoning as /exams/
/textbook/
